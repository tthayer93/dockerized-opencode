# Match the container user to your host UID/GID so bind mounts have correct ownership.
PUID=$(id -u)
PGID=$(id -g)

# Web UI authentication (leave empty for no auth).
OPENCODE_SERVER_USERNAME="opencode"
OPENCODE_SERVER_PASSWORD=""
